{
  "name": "iresene",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "publish": "npm run format && npm run boot && npm run init && npm run build && npm run test && lerna publish",
    "test": "NODE_OPTIONS=--experimental-vm-modules jest",
    "init": "npm install && lerna run init",
    "boot": "lerna bootstrap --hoist",
    "build": "node scripts/build.cjs",
    "rm-build": "rm -rf ./packages/core/dist && rm -rf ./packages/react-query/dist && rm -rf ./packages/object-path/dist && rm -rf ./packages/helpers/dist",
    "format": "prettier --write \"./**/*.{js,mjs,cjs,md,json}\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@babel/core": "^7.13.1",
    "@babel/plugin-proposal-class-properties": "^7.13.0",
    "@babel/preset-env": "^7.13.5",
    "babel-loader": "^8.2.2",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "prettier": "^2.2.1",
    "webpack": "^5.24.2",
    "webpack-cli": "^4.5.0"
  }
}
